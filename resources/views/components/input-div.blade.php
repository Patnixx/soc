<div class="form-group">
    <label for="{{$label}}">{{$label}}</label>
    <input type={{$type}}" name="{{$name}}" id="{{$id}}" class="" placeholder="{{$placeholder}}" value="{{$value}}"required>
        <i class="{{$icon}} text-white"></i>
</div>